# Nixpacks configuration for Railway
# Let nixpacks handle Python automatically

[phases.setup]
nixPkgs = ["python311", "gcc", "libffi", "openssl"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
